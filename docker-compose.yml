services:
  web:
    container_name: web
    build:
      context: .
      dockerfile: ./src/apps/web/Dockerfile
    restart: always
    ports:
      - "8080:80"
  api:
    container_name: api
    build:
      context: .
      dockerfile: ./src/apps/api-server/Dockerfile
    restart: always
    env_file: ./src/apps/api-server/.env
    environment:
      QDRANT_URL: http://qdrant:6333
    ports:
      - "4000:4000"
    depends_on:
      - qdrant
  qdrant:
    container_name: qdrant
    image: qdrant/qdrant:v1.17.1
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_data:/qdrant/storage

volumes:
  qdrant_data:
    name: map-of-science-qdrant-data