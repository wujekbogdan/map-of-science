---
"@map-of-science/web": minor
---

Replace the old manually edited `foreground.svg` with a new SVG generated based on cluster data produced by an experimental Canvas map renderer. See:: https://github.com/wujekbogdan/map-of-science/pull/73

The old `foreground.svg` image is still being used, but only as a source of label information.
