---
"@map-of-science/cli": minor
---

- Re-enable the `scrape-eto` command. It parses the ETO `cluster_details` JSONL dataset into NDJSON.
- Update the `name` and `ingest:clusters` commands to read the ETO NDJSON's object-shaped article entries.
