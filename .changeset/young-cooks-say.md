---
"@map-of-science/typescript": minor
---

- Bump `typescript` peer and dev to `~6.0.3`.
- Raise `target` and `lib` to `ES2025`.
- Add `types: ["node"]` to the node config.
- Drop `strict` and `module` (now defaults in 6.0).
