---
"@map-of-science/atlas": minor
---

- Export `SortValue`, `sortValueSchema`, and `DEFAULT_SORT`
- Accept an optional `sort` on `Search.query` and re-rank vector results in memory to apply the requested order
