---
"@map-of-science/tsdown": patch
---

- Bump `tsdown` peer and dev to `^0.21.10`.
- Bump `typescript` to `~6.0.3` and add `@types/node` `^22.19.17`.
- Migrate `noExternal` to `deps.alwaysBundle`.
- Set `rootDir` explicitly.
