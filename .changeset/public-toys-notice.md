---
"@map-of-science/atlas": minor
---

- Add `rankRelatedClusters`. It joins a cluster's two citation lists into one list, the strongest link first.
- Add `findByExternalIds` to `ClusterRepository`.
