---
"@map-of-science/web": patch
---

- Bump `vite-plugin-checker` to `^0.13.0`.
- Bump `vite-plugin-svgr` to `^5.2.0`.
- Use a caret range for `@trpc/server` to align with the rest of the workspace.
