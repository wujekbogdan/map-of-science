---
"@map-of-science/api": minor
---

- Return `rankedRelatedClusters` from `cluster.byId`. Each link carries a name.
- Set a link's cluster id to null in `rankedRelatedClusters` when the cluster is not stored.
