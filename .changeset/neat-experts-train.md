---
"@map-of-science/web": patch
---

- Share one cluster test fixture across the map and search specs, in place of six copies.
- Drop the cluster fields the API no longer sends from the test fixtures. No component changed.
