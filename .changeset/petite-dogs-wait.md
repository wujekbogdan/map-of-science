---
"@map-of-science/web": minor
---

- Read the backend API URL and dev-tool flag from the container environment at startup instead of fixing them when the bundle is built.
- Publish a multi-arch Docker image to GHCR on release.
