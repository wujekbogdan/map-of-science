---
"@map-of-science/api": patch
---

- Switch build from `tsc` to `tsdown`.
- Split test into `test:unit`/`test:integration`.
- Add `typecheck` script.
- Add `tsx` watch for dev.
- Add `start` script.
- Upgrade vitest to `^4.0.15`.
