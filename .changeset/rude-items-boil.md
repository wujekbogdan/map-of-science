---
"@map-of-science/vitest": minor
---

- Export TS source directly (remove build step).
- Add `defineReactConfig()` with `happy-dom`.
- Add `unit` + `integration` project separation.
- Upgrade `vitest` to `^4.0.15`.
