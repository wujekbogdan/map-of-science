---
"@map-of-science/web": patch
---

- Accept a cluster from `cluster.byId` next to clusters from `cluster.viewport` and from the search in the map helpers. The three no longer hold the same fields.
