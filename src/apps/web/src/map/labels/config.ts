export const REF_FONT_SIZE = 10;
export const LINE_HEIGHT = 1.15;
export const LABEL_MIN_ZOOM = 0.8;
export const MAX_LABEL_WIDTH_PX = 140;

// How far, in screen pixels, a cluster's label sits above the cluster marker.
// Rendering converts this to world units via the current zoom scale so the
// on-screen distance stays constant as the user zooms.
export const CLUSTER_LABEL_OFFSET_PX = 15;
