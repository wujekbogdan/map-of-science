export const REF_FONT_SIZE = 10;
export const LINE_HEIGHT = 1.15;
export const LABEL_MIN_ZOOM = 9.6;
export const MAX_LABEL_WIDTH_PX = 140;

// Minimum gap, in screen pixels, between a cluster dot's edge and the top
// edge of its label. Applied on top of the per-level dot radius so the visible
// gap is constant across dot sizes.
export const LABEL_DOT_GAP_PX = 14;
