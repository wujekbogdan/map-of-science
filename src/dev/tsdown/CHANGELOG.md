# @map-of-science/tsdown

## 0.1.1

### Patch Changes

- 5b8bea3: - Bump `tsdown` peer and dev to `^0.21.10`.
  - Bump `typescript` to `~6.0.3` and add `@types/node` `^22.19.17`.
  - Migrate `noExternal` to `deps.alwaysBundle`.
  - Set `rootDir` explicitly.

## 0.1.0

### Minor Changes

- 7f733cb: New shared `tsdown` config package. Exports `defineNodeConfig()` with Node.js defaults.
