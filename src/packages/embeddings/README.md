# @map-of-science/embeddings

Text embedding generation with provider abstraction. SDK layer.

## Responsibilities

- Generate text embeddings via configurable providers
- Differentiate between document and query embeddings
- Support Gemini and dummy (test) providers
